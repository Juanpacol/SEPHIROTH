# 2026-08-19 (session 5)

## Session — Full gap inventory + DEBT-010 (overdue shim cleanup)

**Goal**

User asked for a structured plan to close every remaining gap in the
migration. Researched the complete current state first (charter's Phase 5
scope, `ADR-007`/`ADR-009`, every `project-state.yaml` gap, every
unimplemented `feature-registry.md` row, every unvalidated hypothesis
H1–H6, and `DEBT-001/002/003`) before scoping any single cycle of work —
the full list turned out to be 5–7 independent implementation cycles, too
large for one session. This session executed the cheapest, lowest-risk
slice: documentation corrections plus deleting two shims
(`intelligence/agents/{base,workflow}.py`) that were scheduled for Phase 4
deletion and never actually removed.

**Did**

- Full-repo research pass (one research agent) confirming: `ADR-007`
  claims the recovery engine was "executed phase 3" but
  `src/sephiroth/runtime/recovery.py` doesn't exist — the ADR itself was
  wrong, not the gap tracking. Also confirmed `intelligence/agents/{base,workflow}.py`
  were still present despite `removed_in_phase: 4` in `project-state.yaml`
  and Phase 4 being marked done.
- Corrected `ADR-007`'s status line with an explicit dated note (same
  pattern as prior spec corrections).
- Re-tagged 4 `gaps` entries in `project-state.yaml`: recovery engine and
  the dynamic planner move to phase 5 (phase 4 is done, no phase 6
  exists); the confidence/context-engine tuning gaps get `phase: null`
  since they need a live Gemini key the assistant can't provide, not a
  build cycle.
- Deleted `intelligence/agents/base.py` (zero external callers) and
  `intelligence/agents/workflow.py` (5 callers), retargeting all of them
  onto `sephiroth.runtime` directly.
- Deleted the shim-identity test in `test_agent_registry.py`; rewrote
  `test_workflow.py`'s docstring since it's no longer running through a
  shim but is kept permanently as the executor's characterization test.
- `docs/specs/SPEC-003-agent-runtime.md` → v1.1.0, retiring the two
  acceptance criteria tied to the now-deleted shim (same "no literal
  `AC-\d{3}-\d{2}` token in the retirement note" technique used for every
  prior AC retirement).
- New `DEBT-010` entry in `project-state.yaml`, `status: resolved`.

**Decisions**

- **Scoped this session to the cheapest slice only.** The full inventory
  (telemetry/tracing, recovery engine, relocating `risk_engine.py` into
  `src/sephiroth/safety/risk`, the dynamic planner, `DEBT-001/002/003`)
  is presented as a high-level roadmap in the plan file, not detailed to
  implementation level — each is its own cycle with its own spec/ADR,
  tests, and Docker verification, same as every prior phase.
- **Confidence/Context-Engine tuning gaps get no fixed phase number**,
  since closing them isn't a code-writing cycle — it requires the user to
  run `--mode full --record` with a real `GEMINI_API_KEY`.

**Tried and rejected**

- Nothing — followed the approved plan directly.

**Problems**

- None. This was a clean repeat of the DEBT-008/DEBT-009 pattern — same
  shape of deviation (charter scheduled shim deletion one phase early,
  approved scope of that phase didn't include it), same fix (retarget
  callers, delete shim, retire the ACs that checked its identity, one
  phase later than scheduled, not further).

**Results**

- 497 passed, 1 skipped, 92.96% coverage (gate: 87%) — one test removed
  from the prior session's 498 (the shim-identity test).
- `ruff check` / `ruff format --check`: clean.
- Eval CI gate: `Overall: PASS` (unchanged).
- `docs_check.py`: OK, 0 warnings. `export_contracts.py --check`: 24/24
  match (no contract changes this session).
- `bandit`: 0 HIGH-severity/HIGH-confidence findings.
- Zero remaining references to `intelligence.agents.workflow`/`.base` in
  the tree except one accurate historical docstring mention.

**Next**

The full remaining inventory, roughly in dependency order:
1. **Telemetry/tracing** (`src/sephiroth/telemetry/`, `ADR-009`, F-042/F-043)
   — `ExecutionTrace`/`Span` contracts already exist and are tested; nothing
   emits spans yet. Needed before `explainability.py` can become a shim.
2. **Recovery engine + lifecycle state machine** (`src/sephiroth/runtime/recovery.py`,
   F-032/F-033, `ADR-007`) — classify failures, pick retry/fallback/replan/
   abstain, populate `RunState.lifecycle` (never filled today).
3. **Relocate `risk_engine.py`** into `src/sephiroth/safety/risk` (its
   `strangler_target` doesn't exist yet) — then `citation_guard.py`/
   `risk_engine.py`/`explainability.py` can all become shims, deleted in a
   later cycle once telemetry has "demonstrably absorbed" their roles.
4. **Dynamic capability-matching planner** (F-029/F-030) — highest risk,
   touches the frozen SSE `routing` event; do last, with reinforced
   verification.
5. **DEBT-001/002/003** — independent cleanup (delete ~1581 lines of
   vendored, zero-call-site MedCAT; replace vendored MONAI with the
   already-commented `monai>=1.3` pip dependency; document
   `GuidelineDocument`'s unqueried pgvector column as cold storage rather
   than building unrequested ingestion).
6. Real confidence/abstention/context-engine threshold tuning — the
   user's own action, once a `GEMINI_API_KEY` is available.

**Artifacts**

- `docs/08-decisions/ADR-007-explicit-recovery.md` (status correction)
- `docs/specs/SPEC-003-agent-runtime.md` (v1.1.0)
- Branch: `debt-010-shim-cleanup`
