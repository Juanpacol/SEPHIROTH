# 2026-08-19 (session 9)

## Session — Phase 5, Part 4: dynamic capability-matching planner

**Goal**

Fourth of 5 planned cycles, and the highest-risk one — it touches the
frozen `routing` SSE event. Closes `SPEC-003` NG-1: `route_specialists`'s
static key-presence heuristic gets a model-driven sibling,
`route_specialists_dynamic`, gated by a default-off feature flag.

**Did**

- Re-read `docs/00-migration-charter.md` §2 (the frozen `routing` event
  shape) before touching anything — confirmed the shape
  (`{"event": "routing", "agents": [...]}`) doesn't need to change, only
  which strings can populate `agents`.
- Built `route_specialists_dynamic(context, client)` in `planner.py`: one
  `generate_json` call constrained by a JSON Schema enum to the 4 known
  specialist node names, degrading to `route_specialists` on any
  exception, non-dict payload, or empty/all-unknown `agents` list.
- Added `settings.enable_dynamic_planner` (default `False`) and a shared
  `_route(context, client)` helper in `executor.py`, replacing the
  duplicated `route_specialists(context)` call at both entry points.
- Promoted `citation_guard`/`explainability` imports were already done in
  Part 3; this cycle only touched `planner.py`/`executor.py`/`config.py`.
- Wrote `docs/specs/SPEC-008-dynamic-planner.md`, updated
  `feature-registry.md` (F-029 → ✅), `traceability.md` (R-004 → Yes),
  `project-state.yaml` (0.9.0), `CLAUDE.md` (decision #19).

**Decisions**

- **Empty `agents` list is invalid, not "run nothing"** — a model
  returning `{"agents": []}` degrades to the static heuristic rather than
  running zero specialists. An empty consultation is never a valid
  outcome of a routing failure mode.
- **`H1` (unnecessary invocation rate) stays unmeasured** (NG-3) — the
  spec's job is a correct, safe degradation contract; measuring the
  metric needs live traffic with the flag on, which this environment
  can't produce.

**Tried and rejected**

- Nothing structural — the design (additive function + feature flag +
  degrade-on-any-failure) was decided during planning and held up without
  changes during implementation.

**Problems**

- None. The one coverage gap (`planner.py` line 93, the non-dict-payload
  branch) was caught by the coverage report itself, not a bug — added one
  more test (`test_dynamic_planner_degrades_to_static_on_non_dict_payload`)
  and it reached 100%.

**Results**

- 532 passed, 1 skipped, 93.19% coverage (gate: 87%); `planner.py` 100%.
- **Frozen contract suite passes unmodified with the flag at its default
  (off)**: `test_workflow.py`, `test_sse_contract.py`, `test_api_agents.py`
  — direct evidence the change is additive, the core risk this part
  carried.
- `ruff check` / `ruff format --check`: clean.
- Eval CI gate: `Overall: PASS` (unchanged — flag off, same static path
  the eval always exercised).
- `docs_check.py`: OK, 0 warnings, all AC-008-0{1..4} anchors resolved.
- `export_contracts.py --check`: OK, 24/24 — no contract shape change.
- `bandit`: 0 HIGH-severity/HIGH-confidence findings.
- Docker build + smoke test verified from a clean `git worktree`.

**Next (per the approved 5-part plan)**

1. `DEBT-001/002/003` — the last part: vendored MedCAT/MONAI cleanup,
   `GuidelineDocument.embedding` documentation. Independent, no
   dependencies on the prior 4 parts.

**Artifacts**

- `docs/specs/SPEC-008-dynamic-planner.md`
- `src/sephiroth/runtime/planner.py::route_specialists_dynamic`
- Branch: `phase-5-dynamic-planner`
