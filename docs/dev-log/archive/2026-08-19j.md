# 2026-08-19 (session 10)

## Session — Phase 5, Part 5: DEBT-001/002/003 cleanup (plan complete)

**Goal**

Fifth and final part of the approved 5-part plan closing the remaining
SEPHIROTH migration gaps. Independent of the prior 4 parts: delete two
vendored, zero-call-site code trees (MedCAT, MONAI) and document a
never-queried pgvector column as intentional cold storage.

**Did**

- Re-verified both DEBT items' zero-call-site claims by grep before
  deleting anything, rather than trusting the original plan's inventory
  at face value — confirmed exactly 11 files (~1581 lines) under
  `intelligence/nlp/{ner,pipeline,preprocessing}/` and 157 files under
  `intelligence/medical-imaging/`, both matching the plan's estimates.
- **Found during DEBT-002's removal**: `intelligence/mcp/imaging_server.py`
  never imported the vendored MONAI tree at all — its real-inference
  branch already imports the actual `monai`/`torch` pip packages directly
  (both commented out in `requirements.txt`, gated behind
  `settings.monai_model_path`). The vendored copy was pure orphaned
  reference code from day one, not something that got disconnected later.
  Corrected the plan's framing in `feature-registry.md`'s F-013 note and
  the `DEBT-002` entry accordingly — activating real imaging inference
  means installing the pip dependency, not restoring vendored code.
- Deleted both trees with `git rm -r`.
- Rewrote `GuidelineDocument`'s docstring (`data/schemas/__init__.py`) to
  state plainly that no route reads or writes it today, closing `DEBT-003`
  by clarifying intent rather than building an unrequested ingestion
  endpoint.
- Cleaned up now-orphaned `ruff`/`bandit` `exclude`/`exclude_dirs` entries
  pointing at the deleted paths.
- Updated `project-state.yaml`: all 3 `DEBT` entries → `resolved`, removed
  the standalone `intelligence/nlp/ner` component entry, phase 5 → `done`,
  version → `1.0.0` (this closes the whole 5-part plan).

**Decisions**

- **No `monai`/`torch` pip install, no real inference wiring** — that's
  F-013's actual activation work, a distinct, larger change (needs real
  model weights and validation) that this cleanup cycle correctly
  scoped out rather than half-attempting.
- **No ingestion endpoint for `GuidelineDocument`** — no validated product
  need yet; documenting intent honestly beats building speculative
  infrastructure.

**Tried and rejected**

- Nothing — both deletions were confirmed zero-risk by grep before
  touching anything, so there was no false start to walk back from.

**Problems**

- None beyond the DEBT-002 framing correction above, which was a finding,
  not a bug in this session's own work.

**Results**

- 532 passed, 1 skipped, 93.23% coverage (gate: 87%) — unchanged from
  Part 4, confirming neither deleted tree had any test coverage to lose.
- `ruff check` / `ruff format --check`: clean after removing the
  now-dangling exclude entries.
- Eval CI gate: `Overall: PASS` (unchanged).
- `docs_check.py`: OK — no deleted path left referenced anywhere in
  `project-state.yaml`. `export_contracts.py --check`: 24/24, no shape
  change.
- `bandit`: 0 HIGH-severity/HIGH-confidence findings.
- Docker build + smoke test verified from a clean `git worktree`.

**Next**

The approved 5-part plan is complete. Remaining gaps in
`project-state.yaml` are all explicitly deferred pending live traffic or
a live `GEMINI_API_KEY` run (H1/H4/H6 metrics, threshold tuning) — none
are build-cycle work this environment can close further right now.

**Artifacts**

- `docs/project-state.yaml` (version 1.0.0)
- Branch: `phase-5-debt-cleanup`
