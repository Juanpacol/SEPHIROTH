# 2026-08-19 (session 7)

## Session — Phase 5, Part 2: Recovery engine + lifecycle state machine

**Goal**

Second of 5 planned cycles closing the remaining SEPHIROTH gaps. Executes
`ADR-007`: classify a specialist's failure, pick a bounded recovery
action, and populate `RunState.lifecycle` — all typed contracts since
Phase 0, never implemented until now.

**Did**

- Read `FailureCategory`/`RecoveryActionType`/`LifecycleState`
  (`contracts/enums.py`), `Failure`/`RecoveryAction`
  (`contracts/results.py`), `LLMUnavailableError`
  (`models/base.py`), and `PlanStep.max_attempts`'s default before
  designing anything — confirmed no contract shape needs to change.
- Built `src/sephiroth/runtime/recovery.py`: `classify` maps
  `LLMUnavailableError` → `MODEL`, anything else → `AGENT`;
  `decide_recovery` returns `RETRY` for a transient category
  (`MODEL`/`TOOL`) with attempts remaining, `ABSTAIN` otherwise.
- Rewrote `_run_specialist` in `src/sephiroth/runtime/executor.py` as a
  bounded retry loop (`MAX_AGENT_ATTEMPTS=2`) around the existing
  `traced_span`+`agent.run` call — an exhausted specialist now returns an
  empty `AgentResult` instead of raising. Marked `capabilities` as
  `SELECTED` right after `resolve()` in both `run_consultation` and
  `stream_consultation`.
- Wrote `docs/specs/SPEC-007-recovery.md`, updated `ADR-007`'s status line
  (preserving the 2026-08-19 correction note), `project-state.yaml`
  (version 0.7.0), `feature-registry.md` (F-032/F-033 → ✅),
  `traceability.md` (R-005 → Yes), `CLAUDE.md` (decision #18).

**Decisions**

- **Recovery scoped to specialists only, not the coordinator** (NG-3) —
  matches the approved plan text exactly; the coordinator is the final
  synthesis step with no fallback to substitute, and a partial answer
  without one is a harder, unscoped question.
- **`FALLBACK`/`REPLAN` stay out of scope** (NG-1, NG-2) — one agent per
  capability today, no dynamic planner yet to replan against. Revisit once
  `SPEC-008` exists.

**Tried and rejected**

- First draft of `test_one_specialist_raising_does_not_abort_the_others`
  assumed a generic exception (`AGENT` category) would retry once before
  abstaining — wrong. `decide_recovery`'s transient set is
  `{MODEL, TOOL}`, not `AGENT`; running the test caught this immediately
  (`assert 1 == 2`). Corrected the test to expect immediate abstain for
  `AGENT`, and added a separate new test
  (`test_transient_model_failure_retries_then_succeeds`) to cover the
  retry-then-succeed path the original test could no longer exercise.

**Problems**

- None beyond the self-caught assumption above — the executor rewrite,
  import expansion, and lifecycle-marking insertion all worked cleanly on
  the first attempt.
- `docs_check.py` initially failed: AC-007-05 (frozen SSE contract
  unaffected) had no anchor. Added a one-line docstring note to
  `tests/test_sse_contract.py`'s module docstring pointing at it — same
  pattern used for every prior phase's frozen-file touch.

**Results**

- 519 passed, 1 skipped, 93.17% coverage (gate: 87%); `recovery.py` and
  `executor.py` both 100%.
- `ruff check` / `ruff format --check`: clean.
- Eval CI gate: `Overall: PASS` (unchanged — this phase touches nothing
  the offline eval measures).
- `docs_check.py`: OK, 0 warnings, all AC-007-0{1..5} anchors resolved.
- `export_contracts.py --check`: OK, 24/24 — no contract shape change.
- `bandit`: 0 HIGH-severity/HIGH-confidence findings.
- Docker build + smoke test verified from a clean `git worktree`.

**Next (per the approved 5-part plan)**

1. Relocate `risk_engine.py` → `src/sephiroth/safety/risk`; shim
   `citation_guard.py`/`risk_engine.py`/`explainability.py`.
2. Dynamic capability-matching planner (`SPEC-008`) — highest risk, saved
   for last.
3. `DEBT-001/002/003` — independent, can slot in anywhere.

**Artifacts**

- `docs/specs/SPEC-007-recovery.md`
- `src/sephiroth/runtime/recovery.py`
- Branch: `phase-5-recovery`
