# Dev/test-only tooling — never installed in the production image
# (Dockerfile installs requirements.lock.txt, resolved from requirements.txt
# alone). CI's `test` job installs this file.
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
aiosqlite>=0.20      # SQLite async driver -- tests/conftest.py's in-memory test DB
moto[s3]>=5.0        # mocks S3 for S3BlobStore tests -- no real bucket needed in CI
ruff>=0.5
mypy>=1.10
bandit[toml]>=1.7
pip-audit>=2.7
